apiVersion: apps/v1
kind: Deployment
metadata:
  name: visits-service
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visits-service
  template:
    metadata:
      labels:
        app: visits-service
    spec:
      containers:
        - name: visits-service
          image: {{ .Values.visitsService.image }}:{{ .Values.visitsService.tag }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: {{ .Values.visitsService.port }}
          env:
           - name: CONFIG_SERVER_URL
             value: "http://config-server:8888/"
---
apiVersion: v1
kind: Service
metadata:
  name: visits-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: visits-service
  ports:
    - port: {{ .Values.visitsService.port }}
      targetPort: {{ .Values.visitsService.port }}